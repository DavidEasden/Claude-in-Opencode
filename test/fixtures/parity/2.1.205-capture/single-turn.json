{
  "version": "2.1.205-capture",
  "scenario": {
    "name": "single-turn",
    "description": "Sanitized Claude Code 2.1.205 SDK CLI single-turn request shape derived from the existing raw capture."
  },
  "runtimeInput": {
    "source": "test/fixtures/claude-code-request.json",
    "allowedDifferences": ["baseURL", "apiKey"]
  },
  "expected": {
    "method": "POST",
    "urlKind": "anthropic-messages",
    "headers": {
      "anthropic-version": "2023-06-01",
      "x-api-key": "$API_KEY_REDACTED"
    },
    "body": {
      "model": "claude-opus-4-5-20251101",
      "stream": true,
      "max_tokens": 64000,
      "thinking": { "type": "adaptive" },
      "output_config": { "effort": "high" }
    }
  },
  "redactions": ["headers.x-api-key"],
  "unsupported": ["This seed fixture intentionally covers only top-level request core fields. Later tasks replace it with a full-body fixture generated by the sanitizer."]
}
