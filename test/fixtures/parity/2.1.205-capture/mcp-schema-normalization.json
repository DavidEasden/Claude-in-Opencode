{
  "version": "2.1.205-capture",
  "scenario": {
    "name": "mcp-schema-normalization",
    "description": "Bridge-generated MCP schema normalization scaffold; not a Claude Code golden capture."
  },
  "runtimeInput": {
    "tools": [
      {
        "name": "mcp__demo__cached",
        "description": "Cached",
        "input_schema": { "type": "object", "properties": { "query": { "type": "string" } } },
        "cache_control": { "type": "ephemeral" },
        "eager_input_streaming": true
      }
    ],
    "bridgeOptions": { "runtimeParity": { "enabled": true } }
  },
  "expectedMcpTools": [
    {
      "name": "mcp__demo__cached",
      "description": "Cached",
      "input_schema": { "type": "object", "properties": { "query": { "type": "string" } } },
      "cache_control": { "type": "ephemeral" }
    }
  ],
  "expectedGapReasons": [],
  "unsupported": [
    "Phase 3D MCP fixture is bridge-generated and must be replaced by a sanitized Claude Code 2.1.205 golden capture before declaring external parity."
  ]
}
